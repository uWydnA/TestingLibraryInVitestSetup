import { describe, it, expect } from "vitest";
describe("suite", () => {
  it("test true", async () => {
    expect(true).toBe(true);
  });
  it("test false", async () => {
    expect(false).toBe(false);
  });
});
